# my-app — Minimal BBC-style clone

This is a minimal Next.js project created for MCGS MUN IPC. No categories or complex features included — just a simple homepage and a couple of article pages.

Run locally:

```bash
cd my-app
npm install
npm run dev
```

Open http://localhost:3000
